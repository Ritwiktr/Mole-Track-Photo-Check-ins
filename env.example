# Copy to ".env" in the project root (file is gitignored).
# Do not put real keys in env.example or commit .env.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-4-31b-it
OPENROUTER_HTTP_REFERER=https://moletrack.local
OPENROUTER_APP_TITLE=MoleTrack AI

# In-app purchases (register the same IDs in App Store Connect & Google Play):
#   com.moletrack.app.premium.monthly
#   com.moletrack.app.premium.yearly
#   com.moletrack.app.premium.lifetime
# Bundle / application ID: com.moletrack.app
